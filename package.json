{
  "name": "crave",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.12.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun run build",
    "start": "bun run start",
    "dev": "bun run --filter backend dev & bun run --filter frontend dev" 
  }

}
